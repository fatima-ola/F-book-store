import React from "react";
import { NavBar, HeroSection } from "../components";

const Welcome = () => {
  return (
    <React.Fragment>
      <NavBar />
      <HeroSection />
    </React.Fragment>
  );
};

export default Welcome;
